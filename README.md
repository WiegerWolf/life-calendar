# Life Calendar

This is a plain static site (no Next.js build).

## Local preview

Open `index.html` directly in your browser, or run any static server.

## Deploy

- GitHub Actions workflow: `.github/workflows/deploy-pages.yml`
- Custom domain: `life.tsatsin.com` via `CNAME`
- GitHub Pages serves the repository artifact directly
